{
  "manifest_version": 3,
  "name": "URL Collector",
  "version": "1.0.0",
  "description": "Collect URLs from all open tabs and display them in a popup.",
  "permissions": ["tabs"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Collect URLs"
  }
}

